
After setting the HipKittens path:
```bash
source env.src
```

Run:
```bash
bash mi350x_benchmark.sh
bash mi355x_benchmark.sh
```

**Warning:** Correctness differences are due to dropout. Set dropout to 0.0 to check correctness and set dropout to > 0.0 to benchmark.
